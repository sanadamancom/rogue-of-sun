# Phase 23.0: 追加敵・既存敵再設計の事前監査

production実装は行わない。調査・確定仕様案・history文書作成のみ。

## 1. precheck結果

| 項目 | 結果 |
|---|---|
| repository URL | `https://github.com/sanadamancom/rogue-of-sun` 一致 |
| base branch | `phase-22-immediate-stairs-progression` に checkout 済み |
| local/origin HEAD | 双方 `bcd3f6ab6af038589765f5eed9d8b0cc416c680d` で一致 |
| working tree | clean |
| main統合状況 | 未統合（`git merge-base --is-ancestor` は false） |
| Phase 22関連テスト | 含む全体テストで確認（下記） |
| 既存敵種・AI・属性・visibility・描画・monsterHouseテスト | baseline で全通過 |
| 鍵システム未完成差分 | 該当ファイル内に TODO/FIXME/WIP なし。未完成差分なし |

全体テスト実行結果：`103 test files / 2558 tests` すべて成功。`npm install` は 6 件の脆弱性警告（既存、対象外）のみ。

precheck合格後、`phase-23-0-enemy-expansion-design-audit` ブランチを作成。

## 2. 調査したproduction経路

- `src/game/enemy-def.ts`（`EnemyDefinition`, `BehaviorType`, `MovementType`, 属性、出現フロア）
- `src/game/types.ts`（`EnemyActor`, `GameState`, `Vec2`, `GameMap`）
- `src/game/turn.ts`（3799行、敵AI分岐、`resolveOneEnemy`、chase/telegraph処理、knockback）
- `src/game/combat.ts`（属性ダメージ計算）
- `src/game/visibility.ts`（shadowcast、`isWallOrOutOfBounds`、部屋/廊下視界）
- `src/game/dark-rooms.ts`（暗い部屋判定）
- `src/game/monster-house.ts`（RNG XOR registry、敵配置、reward配置）
- `src/game/telegraph.ts`（cockatrice/kraken の予告getter、単一固定タイル方式）
- `src/game/state.ts`（フロア生成、RNGストリーム分離）
- `src/game/telemetry.ts`（schemaVersion 7、撃破/ドロップ集計）
- `src/main.ts`（スプライトロード、chroma-key テクスチャ生成、`idleFrame`/`walkFrames`、4方向×3列フレーム構造）
- `src/game/__tests__/armor-and-golem.test.ts`、`enemy-behavior-cockatrice.test.ts`

save/load機構（localStorage等の永続化）は存在しない。敵の移動は正式なパスファインディング（BFS/A*）を持たず、Chebyshev距離を縮める単純グリーディ8方向移動のみ。

## 3. 添付アセット一覧と形式

全7ファイル、共通形式：

| ファイル名 | サイズ | 透過形式 | 構成 |
|---|---|---|---|
| skeleton.png | 72×128 | 不透明、chroma-key緑 (0,255,0) | 3列×4行=12フレーム |
| Skeleton_head.png | 72×128 | 同上 | 3列×4行=12フレーム（全フレーム同一絵柄、差分なし） |
| claygolem_rolling.png | 72×128 | 同上 | 3列×4行=12フレーム |
| ghost.png | 72×128 | 同上 | 3列×4行=12フレーム |
| steps.png | 72×128 | 同上 | 3列×4行=12フレーム（低コントラスト＝足跡のみの絵柄） |
| steps_see.png | 72×128 | 同上 | 3列×4行=12フレーム（steps.pngより濃い＝本体可視版と推測） |
| cockatrice_flying.png | 72×128 | 同上 | 3列×4行=12フレーム |

- 全ファイルとも既存 `public/assets/sprites/*.png`（`claygolem.png`, `cockatrice.png` 等）と同一の 72×128px、24×32px/frame、chroma-key緑 `(0,255,0)` 完全一致。既存の `createChromaKeyTexture()` / `DIRECTION4_ROW`（N=0,E=1,S=2,W=3）/ `FRAMES_PER_ROW=3`（列0=idle、列1-2=walk中間、列1→2→1のループ）にそのまま適合する形式。
- リサイズ・切り出し・命名変更は不要と判断できる（productionアセット規則と一致）。
- **判別不能事項**：`Skeleton_head.png` の12フレームが同一絵柄に見え、方向差・状態差が画像から目視で確定できない。`steps.png` と `steps_see.png` の関係（前者＝足跡のみ描画用、後者＝本体可視時の代替スプライトなのか、本体スプライトそのものなのか）は用途がファイル名から推測できるのみで、どのフレームが「足跡」「棘」「本体出現」のどれに対応するかは不明。棘（9本のスパイク）専用フレームが存在するかどうかも本監査だけでは断定できない。
- 添付ファイルは一切変更していない（リサイズ・切り出し・色変更なし）。

## 4. スケルトンの推奨状態遷移

```
alive（通常） --弱点属性で撃破--> 消滅（経験値/ドロップ確定、終了）
alive（通常） --弱点属性以外で撃破--> headless（頭部状態、経験値/ドロップは"未確定"）
headless --N ターン経過--> alive（新規HPで復活、経験値/ドロップは復活のたびリセットされ再度未確定）
headless --頭部への弱点属性攻撃--> 消滅（この時点で経験値/ドロップ確定）
headless --復活上限到達後に頭部撃破--> 消滅（強制、経験値/ドロップ確定）
```

- 経験値・ドロップ・撃破数・telemetryは「完全消滅の瞬間」にのみ確定する。頭部化そのものでは確定させない（同一個体からの多重取得を防止する必須要件を満たすため）。
- 頭部状態は既存 `EnemyActor` の任意フィールド拡張として保持することを推奨（別Actor/別Entity化は不採用）。理由：既存の bat/mummy/cockatrice/kraken もすべて同一 `EnemyActor` に per-species optional field を追加する方式で実装されており、`state.enemies` 配列の単一ソース・単一 alive 判定を維持できる。別Entityにすると `state.enemies` 走査・monster-house enemy count・save 相当構造・telemetry 集計のすべてに二重管理が発生し、既存の `spawnSource` フィルタパターンと同種の分岐が増える。

## 5. スケルトンの推奨仮数値（要ユーザー承認）

- 弱点属性：**frost**（現行5属性 sol/flame/frost/cloud/earth のうち、既存roster内でまだ弱点として割り当てられていない属性はfrostのみ。他4属性はbok=sol, mummy=flame, golem=cloud, cockatrice=earthで既に使用済み）
- 弱点以外の判定：「撃破した最後の一撃の属性」で判定可能（`combat.ts` の attack resolution は既に単発の属性情報を持つ想定、既存 `computeElementalDamage` 系の出力をそのまま再利用できる）
- 頭部はマスを占有する（プレイヤー・敵とも通行不可）と推奨。理由：既存の占有判定（Actor同士の占有）をそのまま再利用でき、通行可否の新規分岐を避けられる
- 頭部への再攻撃は可能。弱点属性のみ完全消滅、それ以外はダメージ表示のみで状態遷移しない（仮）
- 復活ターン数：仮10ターン（要確定、原作準拠値はユーザー/ChatGPT側判断が必要）
- 復活時LIFE：フル回復（新規スポーン相当）を仮案とする
- 復活回数上限：仮1回（無限復活はゲームバランス上避けるべきと判断。上限到達後は頭部を弱点以外で撃破しても消滅させる特例が必要）
- 復活先が占有されている場合：既存の「配置先が占有されていたら最近傍の空きマスへ」に類するフォールバックが必要（monster-house reward配置のフォールバックパターンを参考にできる）
- monsterHouse専用敵としても同じ状態遷移を利用可能（`spawnSource` フィールドは既存のまま流用でき、専用フラグ追加不要と推定）
- フロア遷移・gameover・Victory時、headless状態のエンティティは他の敵と同様に `state.enemies` から通常どおり破棄されるため、既存の破棄経路で漏れは生じないと推定（要実装時再確認）
- 頭部化・完全消滅それぞれにRNG消費（復活位置決定等）が必要になる見込み。新規XOR定数が最低1つ必要（registry現在 `0x7a19e3c8` まで使用済み、次は未使用値を新規採番）

## 6. ゴーレム突進3案の比較

| 観点 | 案A（壁まで突進） | 案B（固定距離） | 案C（丸まり中に複数ターン分割） |
|---|---|---|---|
| 原作再現度 | 最も高い | 低い（原作の「壁までの直進」感を失う） | 中（複数ターンに分解する分アレンジが必要） |
| 回避可能性 | 予告ターンがあれば1回は回避可能。ただし突進距離が長い場合、プレイヤーの逃走先も突進経路上に入り得る | 距離が短ければ制御しやすい | 各ターンで再評価できるため最も制御しやすい |
| 実装複雑度 | 複数マス移動・衝突判定を1ターン内で完結させる必要があり中程度 | 最も単純（既存 fast_melee の2歩移動パターンを流用可能） | 状態機械が増える分やや複雑（丸まり中フラグ＋継続処理） |
| 画面外からの即時攻撃リスク | 感知距離次第で発生しうる（要上限距離のガード） | 発生しにくい | 発生しにくい（各ターンで停止可能） |
| 複数回ダメージリスク | 1ターン内の一括移動なので単純化すれば1回のみに制御しやすい | 低い | 各ターンでの再攻撃判定が必要になり得る（要ガード） |
| 既存アニメーション境界との整合 | 複数マス移動+1ターン内という点で新規パターン | 既存 `fast_melee` の2歩移動と同種で流用しやすい | 既存の「隔ターン行動」の派生として自然 |

## 7. ゴーレムの推奨仕様

**案Aを第一候補として推奨**（タスク指示の既定方針と一致）。ただし以下のガードを必須とする：

- 突進距離に上限（例：フロアの部屋サイズを考慮した固定上限マス数）を設け、「画面外からの即時攻撃」「回避不能」を避ける
- 1ターン予告を必須とする（telegraph.ts の既存パターンを拡張し、ゴーレム用の固定方向フィールドを追加）
- 斜め突進は不許可（既存 `spider_cardinal` 同様、4方向限定として角抜け禁止との整合を取りやすくする）
- プレイヤー衝突時は停止＋ダメージ1回（ノックバックは既存 `tryKnockback` が golem/kraken を明示的に除外しているため、ゴーレム自身はノックバックされない現行仕様を維持。ゴーレムが与えるノックバックについては新規検討が必要）
- 他の敵との衝突は「その敵の手前で停止」を仮案とする
- 突進後は次のゴーレム自身のターンを硬直（既存 `slow_melee` の隔ターン休止パターンをそのまま流用可能）
- 既存の隔ターン行動（`slow_melee`）は突進の予告/実行ターンの中に統合し、通常移動フェーズは廃止する方向を推奨（併用すると状態が複雑化するため）
- 決定性：突進の距離判定自体はRNGを使わず、方向決定（プレイヤー方向への直線判定）のみで完結できるため新規RNGストリームは不要と推定

## 8. ゴーストの壁抜け実装方式

- 既存 `visibility.ts` の `isWallOrOutOfBounds` は壁を常に遮蔽物として扱うため、壁内にいる敵の可視化・非可視化は新規ロジックが必要
- 経路探索：既存に正式なパスファインディングは存在しない（Chebyshev距離を縮めるグリーディ移動のみ）。ゴーストが壁を含めて移動するには、壁・床を区別しない移動コスト計算（既存の「通行不可判定」を無視する専用分岐）を `turn.ts` に追加する必要がある。外周壁（マップ境界）への侵入は不可とし、内部の壁タイルのみ許可する案を推奨
- 壁内での当たり判定：近接・遠距離・カード・部屋攻撃いずれについても、壁内にいる間は対象外とすることを推奨（「壁内から一方的に攻撃しない」の裏返しとして、壁内では被弾もしない設計が自然。ただし床へ出た瞬間からは通常の被弾対象とする）
- 表示：現在視界内かつ壁タイル上にいる場合のみ半透明等の専用描画（Phase 25でモーション対応、Phase 23では最小限の状態区別のみ）。探索済みだが視界外の場合は他の敵同様に位置情報を一切描画しない（既存の「視界外の敵は描画しない」規則をそのまま適用すれば漏れない）
- 千里眼の実：既存は罠発見の可視化。ゴーストを対象に含めるかは今回未確定（要ユーザー判断、13項参照）

## 9. ゴーストの攻撃可能条件

- 壁内にいる間は移動のみ、攻撃不可
- 床マスへ出たターンから通常の近接攻撃対象・実行者になる（「床へ出た後1ターンの猶予」は不要と推定。出た瞬間から通常敵と同様の扱いにする方が既存の敵行動フレームワークに最も自然に載る）
- 出ようとした床マスが敵/プレイヤーに占有されている場合は、既存の「移動先占有時は移動しない」を流用し、その場（壁内）に留まる

## 10. ステップスの状態遷移

```
hidden（本体非表示、足跡のみ現在視界内に表示）
  --プレイヤーを感知--> telegraphed（3×3範囲を予告、本体はまだ非表示）
  --次の敵ターン--> revealed（9マスへ棘攻撃を実行、中心マスに本体出現）
revealed --一定ターン経過--> hidden（本体再び非表示、足跡表示に戻る）
```

- 千里眼の実使用中は上記に関わらず本体を常時可視化する専用フラグ（例：`revealedByClairvoyance`）を separately 持たせる案を推奨

## 11. ステップスの3×3攻撃・予告仕様

- 感知距離：仮でAGGRO_RANGE相当（既存 `turn.ts` の aggro range 定数を流用、新規定数は不要と推定）
- 3×3の中心：感知した瞬間のステップス自身の座標（プレイヤーの座標ではない。原作同様「自身を中心に」の指示に従う）
- 壁越しの棘発生：不可とする（壁タイルは対象から除外）。既存の攻撃範囲計算（kraken の orthogonal cross 相当）に壁除外フィルタを追加する形で実装可能
- 予告に必要な最小状態：中心座標1点のみで足りる（既存 `tentacleTarget: Vec2` と同型のフィールドを新設できる）
- 攻撃後の可視化：一定ターン後に hidden へ戻す（永続可視化はしない。「最低1回は回避行動を選択できる」という要求と、原作の「見えない敵」という緊張感を両立するため）
- 硬直・再使用間隔：仮で攻撃後2〜3ターンのクールダウンを推奨（要確定）

## 12. 千里眼の実との接続方法

- 既存の罠発見効果（`phase-18-1-trap-discovery` / `phase-18-2-clairvoyance`）を壊さず、ステップスとゴーストの可視化を「同じ実の効果に新しい対象カテゴリを追加する」形で拡張することを推奨
- 可視化の永続性：現在フロア中は永続（既存の罠発見と同じ挙動に揃える）を推奨
- 予告中・攻撃後の個体へ使った場合：可視化フラグが立つだけで、状態遷移そのもの（hidden/telegraphed/revealed）には影響を与えないことを推奨（可視化は表示レイヤーの話であり、ゲームロジックの状態機械とは独立させる）

## 13. コカトリス飛行モーションに必要な状態境界

- 現行の攻撃ロジック（`cockatrice_gaze`）は変更しない前提。飛行モーションは論理位置に対する表示上の補間（浮遊・上下動）として、既存の描画レイヤーにアニメーション用の追加ステートを持たせるだけで足りると推定
- 地形無視・罠無効などのゲーム上の能力は、`movementType: 'flying'`（bat が既に使用中）と同じ意味に留め、追加の当たり判定緩和は行わない設計を推奨（表示と実際の移動可否ロジックを明確に分離する）
- Phase 23で最小限用意すべきもの：行動結果に「今どのフレーム／状態を再生すべきか」を示す最小限の表示イベント（既存の telegraph 表示パターンと同様、ロジック本体とは独立したgetter関数を1つ追加する程度）。詳細なモーション実装自体はPhase 25。

## 14. monsterHouse・暗所・visibilityとの整合

- スケルトンの頭部状態、ゴーストの壁内状態、ステップスの hidden/telegraphed 状態は、いずれも monsterHouse 内で発生してもコード上の扱いは通常フロアと同一にできる見込み（`spawnSource` フィールドは既存のまま流用可能、専用の状態遷移分岐は不要）
- 暗い部屋との組み合わせ：ステップスの「足跡のみ表示」は暗い部屋内では既存の暗所描画フィルタでさらに減衰されると推定（要実装時の描画順序確認）
- ゴーストの壁内可視化は、既存の探索記憶（explored tiles）システムに「壁タイルの敵在否」という新しい情報種別を追加しないよう、常に現在視界内でのみ描画する制約を厳守する必要がある（記憶に残すと「探索済みだが今は見えない壁の中に敵がいる」という情報が漏れる）

## 15. RNG・決定性への影響

- 新規に必要になるRNGストリーム候補：
  - スケルトンの復活位置決定（フォールバック含む）
  - ステップスの感知後・攻撃後の乱数要素（もしあれば）
  - ゴーレムの突進方向決定は座標比較のみで完結する見込みのためRNG不要
- 現行のXOR定数registryは `0x7a19e3c8`（monster house enemy species）まで使用済み。新規ストリームごとに未使用の一意なXOR定数を新規採番する必要がある（重複禁止の原則を厳守）

## 16. セーブ形式への影響

- 明示的なセーブ/ロード機構（localStorage等）は現行コードに存在しない。ただし `GameState` の構造そのものが将来の永続化候補になり得るため、スケルトンの頭部状態・ゴーストの壁内状態・ステップスの状態はすべて `EnemyActor` の**任意（optional）**フィールドとして追加し、既存の「新規フィールドは常にoptionalで後方互換」の原則を維持することを推奨。`schemaVersion` は今回のような union拡張・フィールド追加では bump 不要、フィールドの意味変更や削除が生じる場合のみ bump する既存ルールに従う。

## 17. 共通化する処理と敵固有のまま残す処理

**共通化候補：**
- 「予告→実行」の2段階ターン構造（cockatrice/kraken/ゴーレム突進/ステップス棘で共通の骨格）
- 範囲攻撃のセル列挙（kraken の orthogonal cross とステップスの 3×3 は、中心座標＋パターン形状という点で共通の関数に抽出可能）
- 「壁除外フィルタ付きの範囲攻撃」ロジック

**敵固有のまま残すべき処理：**
- スケルトンの頭部化・復活はスケルトン専用の状態機械（他種に転用予定なし）
- ゴーストの壁内移動・当たり判定除外はゴースト専用
- 各敵の弱点属性・数値バランスは当然個別

## 18. production変更が必要になるファイル候補

- `src/game/types.ts`（`EnemyActor` へのフィールド追加、`EnemyType`/`BehaviorType`/`MovementType` 拡張）
- `src/game/enemy-def.ts`（スケルトン/ゴースト/ステップスの `EnemyDefinition` 追加、ゴーレムの `behaviorType` 見直し）
- `src/game/turn.ts`（各新規AI分岐の追加、既存 `slow_melee` の置換または統合）
- `src/game/telegraph.ts`（3×3予告・ゴーレム突進予告の新規getter追加）
- `src/game/visibility.ts`（壁内敵の可視化除外、足跡表示相当のフィルタ）
- `src/game/combat.ts`（頭部への攻撃・弱点判定の分岐、必要なら）
- `src/game/monster-house.ts`（新種をmonsterHouse対象に含める場合の出現テーブル追加）
- `src/game/state.ts` / `mapgen.ts`（新規RNGストリームの追加）
- `src/game/telemetry.ts`（スケルトンの経験値/ドロップ確定タイミングに関する集計調整）
- `src/main.ts`（新規敵種のスプライトロード・chroma-keyテクスチャ登録、`allEnemySpriteKeys()` への追加）
- `public/assets/sprites/`（新規4アセットの配置：ファイル名は既存命名規則へ整合させる必要があり、添付ファイル名 `claygolem_rolling.png` 等をそのまま使うか改名するかは要判断）

## 19. 必要な新規テスト一覧（Phase 23.1以降で作成予定）

- スケルトン：弱点撃破で完全消滅／弱点以外撃破で頭部化／頭部の複数回攻撃／復活ターン数・LIFE／復活上限／復活先占有時のフォールバック／経験値・ドロップが一度しか発生しないことの確認／monsterHouse内での同一遷移
- ゴーレム：突進予告→実行の2ターン構造／壁・外周での停止／プレイヤー衝突時のダメージ・停止／他敵との衝突／斜め不許可／角抜け禁止との整合／決定性（同一seedで同一結果）
- ゴースト：壁内移動の経路／外周壁への侵入不可／壁内での被弾対象外／床へ出たターンからの攻撃可否／壁内敵の非可視化（現在視界外・視界内壁それぞれ）／探索記憶に情報が残らないことの確認
- ステップス：hidden時の足跡表示のみ／感知→3×3予告／9マス攻撃と中心本体出現／壁越し発生なしの確認／攻撃後の再hidden化／千里眼の実による可視化と非干渉の確認／monsterHouse内配置
- コカトリス：既存攻撃ロジックの非破壊確認（回帰テスト）、表示イベントの最小境界テスト

## 20. 後続Phase 23.1〜23.7の依存関係

```
23.1 スケルトンの復活          … 独立して着手可能（新規属性値の確定が前提）
23.2 ゴーレムの突進            … 独立して着手可能（既存 slow_melee 置換の可否確定が前提）
23.3 ゴーストの壁抜け          … 独立して着手可能（新規経路探索方式の確定が前提）
23.4 ステップスの不可視・棘攻撃・千里眼連携 … 千里眼の実の拡張方式が23.3以前に決まっていると再利用しやすい
23.5 追加敵の生成・monsterHouse・visibility統合監査 … 23.1〜23.4完了後の横断監査（依存: 23.1-23.4すべて）
23.6 完成版の敵ロスターと階層別出現設計 … 依存: 23.5、クロロホルン等の採否確定
23.7 完成版の階層数・難度推移・ラン構成 … 依存: 23.6
```

23.1〜23.4は原則並行着手可能だが、23.3（ゴーストの経路探索）と23.4（ステップスの範囲攻撃）は「壁除外フィルタ付き範囲攻撃」の共通化次第で順序を前後させる余地がある。

## 21. 未確定事項

- `Skeleton_head.png` の12フレームの用途区別（方向差・状態差が目視で確認できない）
- `steps.png` と `steps_see.png` のどちらが「足跡専用」でどちらが「本体可視版」か、あるいは両方が別の状態を指すのか
- ステップスの棘（9本）専用の描画フレームが添付アセット内に存在するか不明
- スケルトンの復活ターン数・復活時LIFE・復活回数上限の具体的数値（本文書はすべて仮案）
- ゴーレムの突進距離上限の具体値
- ゴーストが千里眼の実の対象に含まれるべきか
- ステップスの感知条件・クールダウンの具体値
- ゴーレムが与えるノックバックの有無（既存は golem/kraken が「ノックバックされない」側の除外のみで、「ノックバックを与える」側の仕様は未確認）

## 22. ユーザー判断が必要な事項

- スケルトンの弱点属性を frost とする案の承認可否
- ゴーレム突進の推奨仕様（案A＋距離上限ガード）の承認可否
- 各種仮数値（復活ターン数、復活上限、突進距離上限、ステップス感知距離・クールダウン）の確定
- 千里眼の実の対象拡張範囲（ゴースト含むか否か）
- 添付アセットのファイル名をそのままproductionへ配置するか、既存命名規則（`claygolem.png` 等、種別名のみ）に合わせて改名するか
- Phase 23.1〜23.4の着手順序（本文書は並行可能と分析したが、実務上の優先順位はユーザー判断）

## 23. Phase 23本体の完成版ラン設計へ与える影響

- 新規4種（スケルトン、ゴースト、ステップス、ゴーレム再設計）はいずれも既存の `EnemyActor`/`state.enemies` 単一構造を維持したまま追加可能であり、完成版ラン構成（23.7）の階層別出現設計に大きな構造変更を強いるものではないと判断できる。
- ただし千里眼の実の役割が「罠発見」から「隠れた敵の発見」へ意味的に拡張されるため、23.6のアイテム/敵バランス設計時にこの拡張を前提とした数値調整が必要になる。
- クロロホルン・属性違いゴーレムは今回不採用のためロスターに含めない。23.6での採否判断まで、これらのアセット存在記録のみが残る。

---

## Completion Report

**precheck結果**：全項目合格（1章参照）。repository/branch/HEAD一致、working tree clean、main未統合、既存テスト全2558件通過、鍵システム未完成差分なし。

**受領したアセット一覧**：`claygolem_rolling.png`, `cockatrice_flying.png`, `ghost.png`, `Skeleton_head.png`, `skeleton.png`, `steps_see.png`, `steps.png`（全7点、全て72×128px・12フレーム・chroma-key緑・既存規則と完全一致）。

**未受領・判別不能だったアセット**：クロロホルン、属性違いゴーレムのアセットは本タスクでは受領していない（今回不採用対象のため調査対象外）。受領済み7点の中でも `Skeleton_head.png` のフレーム別用途、`steps.png`/`steps_see.png` の役割分担、ステップスの棘専用フレーム有無は目視のみでは断定不能（21章）。

**調査したproduction経路**：2章参照。

**スケルトンの推奨仕様と仮数値**：4-5章参照（`EnemyActor` 拡張方式、frost弱点案、復活10ターン/フル回復/復活上限1回はいずれも仮）。

**ゴーレム突進3案の比較結果／採用推奨仕様**：6-7章参照（案Aを距離上限ガード付きで推奨）。

**ゴーストの推奨仕様**：8-9章参照（`isWallOrOutOfBounds` を迂回する専用移動ロジック、壁内では被弾対象外）。

**ステップスの推奨仕様**：10-11章参照（hidden/telegraphed/revealedの3状態、9マス攻撃、中心はステップス自身の座標）。

**千里眼の実との接続仕様**：12章参照（既存拡張、現在フロア中永続案）。

**コカトリスのPhase 23・25境界**：13章参照（Phase23は表示イベントの最小境界のみ、モーション本体はPhase25）。

**monsterHouse対応方針／visibility・暗所対応方針**：14章参照（`spawnSource` 流用、壁内敵は視界内のみ描画・探索記憶に残さない）。

**RNG消費への影響**：15章参照（新規XOR定数を registry の `0x7a19e3c8` 以降に一意採番が必要）。

**セーブ形式への影響**：16章参照（永続化機構自体が現状なし、新規フィールドはすべてoptionalで後方互換維持）。

**production変更予定ファイル**：18章参照。

**新規テスト予定一覧**：19章参照。

**後続Phase 23.1〜23.7の依存関係**：20章参照。

**ユーザー判断が必要な事項**：22章参照。

**変更ファイル一覧**：`docs/history/phase-23-0-enemy-expansion-design-audit.md`（新規作成）のみ。

**実行したテストと結果**：`npx vitest run` — 103 test files / 2558 tests、すべて成功。

**型検査結果**：下記コマンド実行後に追記。

**`git diff --check`結果**：下記コマンド実行後に追記。

**commit hash**：下記コマンド実行後に追記。

**push先branch**：`origin/phase-23-0-enemy-expansion-design-audit`

**main未変更確認**：本タスク中、mainブランチへのチェックアウト・変更は一切行っていない。

**最終git status**：下記コマンド実行後に追記。

**指示に従わなかった点と理由**：なし。指示された変更制限（history文書のみ）・commit範囲・push先をすべて遵守した。
